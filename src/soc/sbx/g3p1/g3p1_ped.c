/*
 * $Id: ped_c.stg,v 1.13 Broadcom SDK $"
 * $Copyright: (c) 2016 Broadcom.
 * Broadcom Proprietary and Confidential. All rights reserved.$"
 * 
 * g3p1_ped.c: Guadalupe3k V1.0 Ped Configuration
 *
 * DO NOT EDIT THIS FILE!
 * Edits to this file will be lost when it is regenerated.
 *
 */

#include <soc/sbx/g3p1/g3p1_int.h> 
#include <soc/error.h>
#if defined(BCM_CALADAN3_SUPPORT) && defined(BCM_CALADAN3_G3P1_SUPPORT) 
int soc_sbx_g3p1_ped_init(int unit) {
    soc_sbx_caladan3_pd_hdr_info_t info;
    int err = SOC_E_NONE;
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 12;
     info.type = 1; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 2;
     info.type = 3; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 4;
     info.type = 2; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 0;
     info.type = 4; 
     info.ipv4_type = TRUE;
     info.varlen_mod = 1;
     info.varlen_units = 2;
     info.varlen_size = 3;
     info.varlen_posn = 4;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 40;
     info.type = 5; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 8;
     info.type = 10; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 20;
     info.type = 11; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 8;
     info.type = 34; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 2;
     info.type = 33; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 4;
     info.type = 8; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 4;
     info.type = 9; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 0;
     info.type = 62; 
     info.elen_type = TRUE;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 12;
     info.type = 48; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 16;
     info.type = 49; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
 if (SOC_SUCCESS(err)) {
     sal_memset(&info, 0, sizeof(info));        
     info.length = 4;
     info.type = 58; 
     info.varlen_mod = 0;
     info.varlen_units = 0;
     info.varlen_size = 0;
     info.varlen_posn = 0;
     err = soc_sbx_caladan3_ped_hdr_init(unit, &info, FALSE);
 }
    return err;
}
#endif
