/** \file dnx_data_lif.c
 * 
 * MODULE DEVICE DATA - LIF
 * 
 * Device Data
 * SW component that maintains per device data
 * The data is static and won't be changed after device initialization.
 *     
 * Supported data types:
 *     - Define             - a 'uint32' number (a max value for all devices is maintained)
 *     - feature            - 1 bit per each feature (supported/not supported) - support soc properties 
 *     - table              - the data is accessed with keys and/or can maintain multiple values and/or set by soc property
 *     - numeric            - a 'uint32' number that support soc properties
 * 
 * User interface for DNX DATA component can be found in "dnx_data_if.h" and "dnx_data_if_#module#.h"
 * 
 * Adding the data is done via XMLs placed in "tools/autocoder/DeviceData/dnx/.." 
 * "How to" User Guide can be found in confluence. 
 *        
 *     
 * 
 * AUTO-GENERATED BY AUTOCODER!
 * DO NOT EDIT THIS FILE!
 */
/* *INDENT-OFF* */
/*
 * $Copyright: (c) 2016 Broadcom.
 * Broadcom Proprietary and Confidential. All rights reserved.$
 */
#ifdef BSL_LOG_MODULE
#error "BSL_LOG_MODULE redefined"
#endif
#define BSL_LOG_MODULE BSL_LS_BCMDNX_LIF
/**
 * \brief
 * Mark this file as device data internal file
 */
#define DNX_DATA_INTERNAL
/*
 * INCLUDE FILES:
 * {
 */
#include <soc/dnx/dnx_data/dnx_data.h>
#include <soc/dnx/dnx_data/dnx_data_internal.h>
#include <soc/dnx/dnx_data/dnx_data_internal_lif.h>
/*
 * }
 */

/*
 * Extern per device attach function
 */
extern shr_error_e jer2_a0_data_lif_attach(
    int unit);
/*
 * SUBMODULE - GLOBAL_LIF:
 * {
 */
/*
 * global_lif init
 */
/**
 * \brief Set submodule data to default value per device type
 * 
 * \par DIRECT INPUT:
 *   \param [in] unit - Unit #
 * \par INDIRECT INPUT:
 *   * None
 * \par DIRECT OUTPUT:
 *     rv - see 'shr_error_e
 * \par INDIRECT OUTPUT:
 *   \param [out] submodule_data - pointer to submodule data
 * \remark
 *   * None
 * \see
 *   * None
 */
static shr_error_e
dnx_data_lif_global_lif_init(
    int unit,
    dnx_data_submodule_t *submodule_data)
{
    SHR_FUNC_INIT_VARS(unit);

    submodule_data->name = "global_lif";
    submodule_data->doc = "Global lif attributes";
    /*
     * Features
     */
    submodule_data->nof_features = dnx_data_lif_global_lif_feature_nof;
    submodule_data->features = sal_alloc(sizeof(dnx_data_feature_t) * submodule_data->nof_features, "_dnx_data lif global_lif features");
    if (submodule_data->features == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_global_lif features\n");
    }
    sal_memset(submodule_data->features, 0, sizeof(dnx_data_feature_t) * submodule_data->nof_features);

    /*
     * Defines
     */
    submodule_data->nof_defines = dnx_data_lif_global_lif_define_nof;
    submodule_data->defines = sal_alloc(sizeof(dnx_data_define_t) * submodule_data->nof_defines, "_dnx_data lif global_lif defines");
    if (submodule_data->defines == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_global_lif defines\n");
    }
    sal_memset(submodule_data->defines, 0, sizeof(dnx_data_define_t) * submodule_data->nof_defines);

    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_in_lifs].name = "nof_global_in_lifs";
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_in_lifs].doc = "Number of in global lifs in the device.";
    /* Set data type flag */
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_in_lifs].flags |= DNX_DATA_F_DEFINE;

    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_out_lifs].name = "nof_global_out_lifs";
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_out_lifs].doc = "Number of out global lifs in the device.";
    /* Set data type flag */
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_global_out_lifs].flags |= DNX_DATA_F_DEFINE;

    submodule_data->defines[dnx_data_lif_global_lif_define_nof_glem_lines].name = "nof_glem_lines";
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_glem_lines].doc = "Number of entries in the GLEM.                     This is the maximum number of egress global lifs in the system.";
    /* Set data type flag */
    submodule_data->defines[dnx_data_lif_global_lif_define_nof_glem_lines].flags |= DNX_DATA_F_NUMERIC;

    /*
     * Tables
     */
    submodule_data->nof_tables = dnx_data_lif_global_lif_table_nof;
    submodule_data->tables = sal_alloc(sizeof(dnx_data_table_t) * submodule_data->nof_tables, "_dnx_data lif global_lif tables");
    if (submodule_data->tables == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_global_lif tables\n");
    }
    sal_memset(submodule_data->tables, 0, sizeof(dnx_data_table_t) * submodule_data->nof_tables);


exit:
    SHR_FUNC_EXIT;
}

/*
 * global_lif features
 */
int
dnx_data_lif_global_lif_feature_get(
    int unit,
    dnx_data_lif_global_lif_feature_e feature)
{
    return dnx_data_mgmt_feature_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_global_lif, feature);
}

/*
 * global_lif defines
 */
uint32
dnx_data_lif_global_lif_nof_global_in_lifs_get(
    int unit)
{
    return dnx_data_mgmt_define_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_global_lif, dnx_data_lif_global_lif_define_nof_global_in_lifs);
}

uint32
dnx_data_lif_global_lif_nof_global_out_lifs_get(
    int unit)
{
    return dnx_data_mgmt_define_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_global_lif, dnx_data_lif_global_lif_define_nof_global_out_lifs);
}

uint32
dnx_data_lif_global_lif_nof_glem_lines_get(
    int unit)
{
    return dnx_data_mgmt_define_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_global_lif, dnx_data_lif_global_lif_define_nof_glem_lines);
}

/*
 * global_lif tables
 */
/* Table Get */
/* Get value as str */
/*
 * }
 */

/*
 * SUBMODULE - IN_LIF:
 * {
 */
/*
 * in_lif init
 */
/**
 * \brief Set submodule data to default value per device type
 * 
 * \par DIRECT INPUT:
 *   \param [in] unit - Unit #
 * \par INDIRECT INPUT:
 *   * None
 * \par DIRECT OUTPUT:
 *     rv - see 'shr_error_e
 * \par INDIRECT OUTPUT:
 *   \param [out] submodule_data - pointer to submodule data
 * \remark
 *   * None
 * \see
 *   * None
 */
static shr_error_e
dnx_data_lif_in_lif_init(
    int unit,
    dnx_data_submodule_t *submodule_data)
{
    SHR_FUNC_INIT_VARS(unit);

    submodule_data->name = "in_lif";
    submodule_data->doc = "In lif attributes";
    /*
     * Features
     */
    submodule_data->nof_features = dnx_data_lif_in_lif_feature_nof;
    submodule_data->features = sal_alloc(sizeof(dnx_data_feature_t) * submodule_data->nof_features, "_dnx_data lif in_lif features");
    if (submodule_data->features == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_in_lif features\n");
    }
    sal_memset(submodule_data->features, 0, sizeof(dnx_data_feature_t) * submodule_data->nof_features);

    /*
     * Defines
     */
    submodule_data->nof_defines = dnx_data_lif_in_lif_define_nof;
    submodule_data->defines = sal_alloc(sizeof(dnx_data_define_t) * submodule_data->nof_defines, "_dnx_data lif in_lif defines");
    if (submodule_data->defines == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_in_lif defines\n");
    }
    sal_memset(submodule_data->defines, 0, sizeof(dnx_data_define_t) * submodule_data->nof_defines);

    submodule_data->defines[dnx_data_lif_in_lif_define_nof_local_in_lifs].name = "nof_local_in_lifs";
    submodule_data->defines[dnx_data_lif_in_lif_define_nof_local_in_lifs].doc = "Number of local inlifs in the device.";
    /* Set data type flag */
    submodule_data->defines[dnx_data_lif_in_lif_define_nof_local_in_lifs].flags |= DNX_DATA_F_NUMERIC;

    /*
     * Tables
     */
    submodule_data->nof_tables = dnx_data_lif_in_lif_table_nof;
    submodule_data->tables = sal_alloc(sizeof(dnx_data_table_t) * submodule_data->nof_tables, "_dnx_data lif in_lif tables");
    if (submodule_data->tables == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_in_lif tables\n");
    }
    sal_memset(submodule_data->tables, 0, sizeof(dnx_data_table_t) * submodule_data->nof_tables);


exit:
    SHR_FUNC_EXIT;
}

/*
 * in_lif features
 */
int
dnx_data_lif_in_lif_feature_get(
    int unit,
    dnx_data_lif_in_lif_feature_e feature)
{
    return dnx_data_mgmt_feature_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_in_lif, feature);
}

/*
 * in_lif defines
 */
uint32
dnx_data_lif_in_lif_nof_local_in_lifs_get(
    int unit)
{
    return dnx_data_mgmt_define_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_in_lif, dnx_data_lif_in_lif_define_nof_local_in_lifs);
}

/*
 * in_lif tables
 */
/* Table Get */
/* Get value as str */
/*
 * }
 */

/*
 * SUBMODULE - OUT_LIF:
 * {
 */
/*
 * out_lif init
 */
/**
 * \brief Set submodule data to default value per device type
 * 
 * \par DIRECT INPUT:
 *   \param [in] unit - Unit #
 * \par INDIRECT INPUT:
 *   * None
 * \par DIRECT OUTPUT:
 *     rv - see 'shr_error_e
 * \par INDIRECT OUTPUT:
 *   \param [out] submodule_data - pointer to submodule data
 * \remark
 *   * None
 * \see
 *   * None
 */
static shr_error_e
dnx_data_lif_out_lif_init(
    int unit,
    dnx_data_submodule_t *submodule_data)
{
    SHR_FUNC_INIT_VARS(unit);

    submodule_data->name = "out_lif";
    submodule_data->doc = "Out lif attributes";
    /*
     * Features
     */
    submodule_data->nof_features = dnx_data_lif_out_lif_feature_nof;
    submodule_data->features = sal_alloc(sizeof(dnx_data_feature_t) * submodule_data->nof_features, "_dnx_data lif out_lif features");
    if (submodule_data->features == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_out_lif features\n");
    }
    sal_memset(submodule_data->features, 0, sizeof(dnx_data_feature_t) * submodule_data->nof_features);

    /*
     * Defines
     */
    submodule_data->nof_defines = dnx_data_lif_out_lif_define_nof;
    submodule_data->defines = sal_alloc(sizeof(dnx_data_define_t) * submodule_data->nof_defines, "_dnx_data lif out_lif defines");
    if (submodule_data->defines == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_out_lif defines\n");
    }
    sal_memset(submodule_data->defines, 0, sizeof(dnx_data_define_t) * submodule_data->nof_defines);

    submodule_data->defines[dnx_data_lif_out_lif_define_nof_local_out_lifs].name = "nof_local_out_lifs";
    submodule_data->defines[dnx_data_lif_out_lif_define_nof_local_out_lifs].doc = "Number of local outlifs in the device.";
    /* Set data type flag */
    submodule_data->defines[dnx_data_lif_out_lif_define_nof_local_out_lifs].flags |= DNX_DATA_F_NUMERIC;

    /*
     * Tables
     */
    submodule_data->nof_tables = dnx_data_lif_out_lif_table_nof;
    submodule_data->tables = sal_alloc(sizeof(dnx_data_table_t) * submodule_data->nof_tables, "_dnx_data lif out_lif tables");
    if (submodule_data->tables == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif_out_lif tables\n");
    }
    sal_memset(submodule_data->tables, 0, sizeof(dnx_data_table_t) * submodule_data->nof_tables);


exit:
    SHR_FUNC_EXIT;
}

/*
 * out_lif features
 */
int
dnx_data_lif_out_lif_feature_get(
    int unit,
    dnx_data_lif_out_lif_feature_e feature)
{
    return dnx_data_mgmt_feature_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_out_lif, feature);
}

/*
 * out_lif defines
 */
uint32
dnx_data_lif_out_lif_nof_local_out_lifs_get(
    int unit)
{
    return dnx_data_mgmt_define_data_get(unit, dnx_data_module_lif, dnx_data_lif_submodule_out_lif, dnx_data_lif_out_lif_define_nof_local_out_lifs);
}

/*
 * out_lif tables
 */
/* Table Get */
/* Get value as str */
/*
 * }
 */

shr_error_e
dnx_data_lif_init(
    int unit,
    dnx_data_module_t *module_data)
{
    SHR_FUNC_INIT_VARS(unit);

    /* Data Struct Init */
    module_data->name = "lif";
    module_data->nof_submodules = dnx_data_lif_submodule_nof;
    module_data->submodules = sal_alloc(sizeof(dnx_data_submodule_t) * module_data->nof_submodules, "_dnx_data lif submodules");
    if (module_data->submodules == NULL)
    {
        SHR_ERR_EXIT(_SHR_E_MEMORY, "failed to allocate dnx_data lif submodules\n");
    }
    sal_memset(module_data->submodules, 0, sizeof(dnx_data_submodule_t) * module_data->nof_submodules);

    /*
     * Init Submodule data
     */
    SHR_IF_ERR_EXIT(dnx_data_lif_global_lif_init(unit, &module_data->submodules[dnx_data_lif_submodule_global_lif]));
    SHR_IF_ERR_EXIT(dnx_data_lif_in_lif_init(unit, &module_data->submodules[dnx_data_lif_submodule_in_lif]));
    SHR_IF_ERR_EXIT(dnx_data_lif_out_lif_init(unit, &module_data->submodules[dnx_data_lif_submodule_out_lif]));
    /*
     * Attach device module
     */
    if (SOC_IS_JERICHO_2_A0(unit))
    {
        SHR_IF_ERR_EXIT(jer2_a0_data_lif_attach(unit));
    }

exit:
    SHR_FUNC_EXIT;
}
/* *INDENT-ON* */
