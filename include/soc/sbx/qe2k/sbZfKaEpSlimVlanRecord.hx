/*  -*-  Mode:C; c-basic-offset:4 -*- */
/*
 * $Id: sbZfKaEpSlimVlanRecord.hx,v 1.2 Broadcom SDK $
 * $Copyright: (c) 2016 Broadcom.
 * Broadcom Proprietary and Confidential. All rights reserved.$
 */


#ifndef SB_ZF_ZFKAEPSLIMVLANRECORD_H
#define SB_ZF_ZFKAEPSLIMVLANRECORD_H

#define SB_ZF_ZFKAEPSLIMVLANRECORD_SIZE_IN_BYTES 32
#define SB_ZF_ZFKAEPSLIMVLANRECORD_SIZE 32
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NVID0_BITS "255:244"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NVID1_BITS "191:180"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NOFFSET_BITS "179:176"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NVLANPACKETCOUNTER_BITS "127:99"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NNVLANBYTECOUNTER_BITS "98:64"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NDROPPACKETCOUNTER_BITS "63:35"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NNDROPBYTECOUNTER_BITS "34:0"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_0_BITS "193:192"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_1_BITS "195:194"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_2_BITS "197:196"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_3_BITS "199:198"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_4_BITS "201:200"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_5_BITS "203:202"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_6_BITS "205:204"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_7_BITS "207:206"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_8_BITS "209:208"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_9_BITS "211:210"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_10_BITS "213:212"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_11_BITS "215:214"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_12_BITS "217:216"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_13_BITS "219:218"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_14_BITS "221:220"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_15_BITS "223:222"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_16_BITS "225:224"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_17_BITS "227:226"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_18_BITS "229:228"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_19_BITS "231:230"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_20_BITS "233:232"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_21_BITS "235:234"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_22_BITS "237:236"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_23_BITS "239:238"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_24_BITS "129:128"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_25_BITS "131:130"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_26_BITS "133:132"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_27_BITS "135:134"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_28_BITS "137:136"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_29_BITS "139:138"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_30_BITS "141:140"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_31_BITS "143:142"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_32_BITS "145:144"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_33_BITS "147:146"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_34_BITS "149:148"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_35_BITS "151:150"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_36_BITS "153:152"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_37_BITS "155:154"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_38_BITS "157:156"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_39_BITS "159:158"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_40_BITS "161:160"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_41_BITS "163:162"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_42_BITS "165:164"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_43_BITS "167:166"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_44_BITS "169:168"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_45_BITS "171:170"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_46_BITS "173:172"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_47_BITS "175:174"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_48_BITS "241:240"
#define SB_ZF_ZFKAEPSLIMVLANRECORD_M_NPORTSTATE_49_BITS "243:242"


typedef struct _sbZfKaEpSlimVlanRecord {
  uint32 m_nVid0;
  uint32 m_nVid1;
  uint32 m_nOffset;
  uint32 m_nVlanPacketCounter;
  uint64 m_nnVlanByteCounter;
  uint32 m_nDropPacketCounter;
  uint64 m_nnDropByteCounter;
  uint32 m_nPortState_0;
  uint32 m_nPortState_1;
  uint32 m_nPortState_2;
  uint32 m_nPortState_3;
  uint32 m_nPortState_4;
  uint32 m_nPortState_5;
  uint32 m_nPortState_6;
  uint32 m_nPortState_7;
  uint32 m_nPortState_8;
  uint32 m_nPortState_9;
  uint32 m_nPortState_10;
  uint32 m_nPortState_11;
  uint32 m_nPortState_12;
  uint32 m_nPortState_13;
  uint32 m_nPortState_14;
  uint32 m_nPortState_15;
  uint32 m_nPortState_16;
  uint32 m_nPortState_17;
  uint32 m_nPortState_18;
  uint32 m_nPortState_19;
  uint32 m_nPortState_20;
  uint32 m_nPortState_21;
  uint32 m_nPortState_22;
  uint32 m_nPortState_23;
  uint32 m_nPortState_24;
  uint32 m_nPortState_25;
  uint32 m_nPortState_26;
  uint32 m_nPortState_27;
  uint32 m_nPortState_28;
  uint32 m_nPortState_29;
  uint32 m_nPortState_30;
  uint32 m_nPortState_31;
  uint32 m_nPortState_32;
  uint32 m_nPortState_33;
  uint32 m_nPortState_34;
  uint32 m_nPortState_35;
  uint32 m_nPortState_36;
  uint32 m_nPortState_37;
  uint32 m_nPortState_38;
  uint32 m_nPortState_39;
  uint32 m_nPortState_40;
  uint32 m_nPortState_41;
  uint32 m_nPortState_42;
  uint32 m_nPortState_43;
  uint32 m_nPortState_44;
  uint32 m_nPortState_45;
  uint32 m_nPortState_46;
  uint32 m_nPortState_47;
  uint32 m_nPortState_48;
  uint32 m_nPortState_49;
} sbZfKaEpSlimVlanRecord_t;

uint32
sbZfKaEpSlimVlanRecord_Pack(sbZfKaEpSlimVlanRecord_t *pFrom,
                            uint8 *pToData,
                            uint32 nMaxToDataIndex);
void
sbZfKaEpSlimVlanRecord_Unpack(sbZfKaEpSlimVlanRecord_t *pToStruct,
                              uint8 *pFromData,
                              uint32 nMaxToDataIndex);
void
sbZfKaEpSlimVlanRecord_InitInstance(sbZfKaEpSlimVlanRecord_t *pFrame);

#ifdef SAND_BIG_ENDIAN_HOST
#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[28] = ((pToData)[28] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[20] = ((pToData)[20] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_OFFSET(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~0x0f) | ((nFromData) & 0x0f); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[15] = ((pToData)[15] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[14] = ((pToData)[14] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[13] = ((pToData)[13] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[12] = ((pToData)[12] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[11] = ((nFromData)) & 0xFF; \
           (pToData)[10] = ((pToData)[10] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[9] = ((pToData)[9] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[8] = ((pToData)[8] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[15] = ((pToData)[15] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[7] = ((pToData)[7] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[6] = ((pToData)[6] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[5] = ((pToData)[5] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[4] = ((pToData)[4] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[3] = ((nFromData)) & 0xFF; \
           (pToData)[2] = ((pToData)[2] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[1] = ((pToData)[1] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[0] = ((pToData)[0] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[7] = ((pToData)[7] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_8(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_9(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_10(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_11(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_12(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_13(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_14(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_15(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_16(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_17(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_18(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_19(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_20(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_21(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_22(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_23(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_24(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_25(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_26(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_27(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_28(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_29(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_30(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_31(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_32(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_33(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_34(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_35(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_36(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_37(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_38(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_39(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_40(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_41(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_42(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_43(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_44(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_45(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_46(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_47(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_48(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_49(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#else
#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[31] = ((pToData)[31] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[23] = ((pToData)[23] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_OFFSET(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~0x0f) | ((nFromData) & 0x0f); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[12] = ((pToData)[12] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[13] = ((pToData)[13] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[14] = ((pToData)[14] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[15] = ((pToData)[15] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[8] = ((nFromData)) & 0xFF; \
           (pToData)[9] = ((pToData)[9] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[10] = ((pToData)[10] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[11] = ((pToData)[11] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[12] = ((pToData)[12] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[4] = ((pToData)[4] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[5] = ((pToData)[5] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[6] = ((pToData)[6] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[7] = ((pToData)[7] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[0] = ((nFromData)) & 0xFF; \
           (pToData)[1] = ((pToData)[1] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[2] = ((pToData)[2] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[3] = ((pToData)[3] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[4] = ((pToData)[4] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_8(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_9(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_10(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_11(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_12(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_13(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_14(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_15(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_16(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_17(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_18(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_19(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_20(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_21(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_22(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_23(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_24(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_25(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_26(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_27(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_28(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_29(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_30(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_31(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_32(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_33(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_34(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_35(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_36(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_37(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_38(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_39(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_40(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_41(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_42(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_43(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_44(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_45(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_46(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_47(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_48(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_49(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#endif
#ifdef SAND_BIG_ENDIAN_HOST
#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[28] = ((pToData)[28] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[20] = ((pToData)[20] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_OFFSET(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~0x0f) | ((nFromData) & 0x0f); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[15] = ((pToData)[15] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[14] = ((pToData)[14] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[13] = ((pToData)[13] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[12] = ((pToData)[12] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[11] = ((nFromData)) & 0xFF; \
           (pToData)[10] = ((pToData)[10] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[9] = ((pToData)[9] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[8] = ((pToData)[8] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[15] = ((pToData)[15] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[7] = ((pToData)[7] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[6] = ((pToData)[6] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[5] = ((pToData)[5] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[4] = ((pToData)[4] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[3] = ((nFromData)) & 0xFF; \
           (pToData)[2] = ((pToData)[2] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[1] = ((pToData)[1] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[0] = ((pToData)[0] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[7] = ((pToData)[7] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_8(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_9(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_10(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_11(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_12(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_13(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_14(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_15(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_16(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_17(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_18(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_19(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((pToData)[31] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_20(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_21(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_22(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_23(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_24(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_25(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_26(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_27(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_28(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_29(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_30(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_31(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_32(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_33(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_34(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_35(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_36(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_37(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_38(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_39(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_40(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_41(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_42(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_43(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((pToData)[23] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_44(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_45(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_46(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_47(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_48(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_49(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#else
#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[31] = ((pToData)[31] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VID1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x0f << 4)) | (((nFromData) & 0x0f) << 4); \
           (pToData)[23] = ((pToData)[23] & ~0xFF) | (((nFromData) >> 4) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_OFFSET(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~0x0f) | ((nFromData) & 0x0f); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[12] = ((pToData)[12] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[13] = ((pToData)[13] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[14] = ((pToData)[14] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[15] = ((pToData)[15] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_VLAN_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[8] = ((nFromData)) & 0xFF; \
           (pToData)[9] = ((pToData)[9] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[10] = ((pToData)[10] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[11] = ((pToData)[11] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[12] = ((pToData)[12] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_PACKET_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[4] = ((pToData)[4] & ~(0x1f << 3)) | (((nFromData) & 0x1f) << 3); \
           (pToData)[5] = ((pToData)[5] & ~0xFF) | (((nFromData) >> 5) & 0xFF); \
           (pToData)[6] = ((pToData)[6] & ~0xFF) | (((nFromData) >> 13) & 0xFF); \
           (pToData)[7] = ((pToData)[7] & ~0xFF) | (((nFromData) >> 21) & 0xFF); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_DROP_BYTE_COUNTER(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[0] = ((nFromData)) & 0xFF; \
           (pToData)[1] = ((pToData)[1] & ~0xFF) | (((nFromData) >> 8) & 0xFF); \
           (pToData)[2] = ((pToData)[2] & ~0xFF) | (((nFromData) >> 16) & 0xFF); \
           (pToData)[3] = ((pToData)[3] & ~0xFF) | (((nFromData) >> 24) & 0xFF); \
           (pToData)[4] = ((pToData)[4] & ~ 0x07) | (((nFromData) >> 32) & 0x07); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_8(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_9(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_10(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_11(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_12(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_13(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_14(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_15(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((pToData)[27] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_16(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_17(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_18(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_19(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_20(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_21(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_22(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_23(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_24(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_25(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_26(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_27(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_28(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_29(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_30(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_31(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_32(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_33(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_34(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_35(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_36(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_37(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_38(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_39(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((pToData)[19] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_40(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_41(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_42(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_43(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_44(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_45(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_46(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 4)) | (((nFromData) & 0x03) << 4); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_47(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_48(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~0x03) | ((nFromData) & 0x03); \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_SET_PORT_STATE_49(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x03 << 2)) | (((nFromData) & 0x03) << 2); \
          } while(0)

#endif
#ifdef SAND_BIG_ENDIAN_HOST
#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[28] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[20] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_OFFSET(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21]) & 0x0f; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[15] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[14] << 5; \
           (nToData) |= (uint32) (pFromData)[13] << 13; \
           (nToData) |= (uint32) (pFromData)[12] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[11]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[10]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[9]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[8]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[15]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[7] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[6] << 5; \
           (nToData) |= (uint32) (pFromData)[5] << 13; \
           (nToData) |= (uint32) (pFromData)[4] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[3]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[2]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[1]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[0]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[7]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_8(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_9(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_10(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_11(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_12(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_13(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_14(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_15(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_16(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_17(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_18(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_19(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_20(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_21(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_22(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_23(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_24(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_25(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_26(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_27(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_28(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_29(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_30(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_31(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_32(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_33(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_34(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_35(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_36(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_37(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_38(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_39(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_40(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_41(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_42(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_43(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_44(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_45(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_46(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_47(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_48(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_49(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 2) & 0x03; \
          } while(0)

#else
#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[31] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[23] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_OFFSET(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22]) & 0x0f; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[12] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[13] << 5; \
           (nToData) |= (uint32) (pFromData)[14] << 13; \
           (nToData) |= (uint32) (pFromData)[15] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[8]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[9]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[10]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[11]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[12]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[4] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[5] << 5; \
           (nToData) |= (uint32) (pFromData)[6] << 13; \
           (nToData) |= (uint32) (pFromData)[7] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[0]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[1]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[2]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[3]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[4]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_8(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_9(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_10(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_11(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_12(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_13(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_14(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_15(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_16(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_17(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_18(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_19(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_20(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_21(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_22(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_23(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_24(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_25(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_26(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_27(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_28(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_29(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_30(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_31(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_32(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_33(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_34(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_35(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_36(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_37(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_38(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_39(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_40(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_41(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_42(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_43(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_44(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_45(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_46(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_47(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_48(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_49(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 2) & 0x03; \
          } while(0)

#endif
#ifdef SAND_BIG_ENDIAN_HOST
#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[28] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[20] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_OFFSET(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21]) & 0x0f; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[15] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[14] << 5; \
           (nToData) |= (uint32) (pFromData)[13] << 13; \
           (nToData) |= (uint32) (pFromData)[12] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[11]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[10]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[9]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[8]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[15]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[7] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[6] << 5; \
           (nToData) |= (uint32) (pFromData)[5] << 13; \
           (nToData) |= (uint32) (pFromData)[4] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[3]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[2]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[1]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[0]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[7]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_8(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_9(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_10(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_11(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_12(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_13(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_14(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_15(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_16(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_17(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_18(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_19(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[31] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_20(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_21(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_22(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_23(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_24(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_25(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_26(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_27(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_28(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_29(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_30(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_31(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_32(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_33(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_34(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_35(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_36(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_37(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_38(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_39(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_40(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_41(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_42(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_43(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[23] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_44(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_45(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_46(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_47(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_48(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_49(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 2) & 0x03; \
          } while(0)

#else
#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[31] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VID1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 4) & 0x0f; \
           (nToData) |= (uint32) (pFromData)[23] << 4; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_OFFSET(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22]) & 0x0f; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[12] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[13] << 5; \
           (nToData) |= (uint32) (pFromData)[14] << 13; \
           (nToData) |= (uint32) (pFromData)[15] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_VLAN_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[8]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[9]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[10]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[11]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[12]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_PACKET_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[4] >> 3) & 0x1f; \
           (nToData) |= (uint32) (pFromData)[5] << 5; \
           (nToData) |= (uint32) (pFromData)[6] << 13; \
           (nToData) |= (uint32) (pFromData)[7] << 21; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_DROP_BYTE_COUNTER(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           COMPILER_64_SET((nToData), 0, (unsigned int) (pFromData)[0]); \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[1]); COMPILER_64_SHL(tmp, 8); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[2]); COMPILER_64_SHL(tmp, 16); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[3]); COMPILER_64_SHL(tmp, 24); COMPILER_64_OR(*tmp0, tmp); }; \
           { VOL COMPILER_UINT64 *tmp0 = &(nToData); COMPILER_64_SET(tmp, 0, (unsigned int) (pFromData)[4]); COMPILER_64_SHL(tmp, 32); COMPILER_64_OR(*tmp0, tmp); }; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_8(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_9(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_10(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_11(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_12(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_13(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_14(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_15(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[27] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_16(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_17(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_18(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_19(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_20(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_21(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_22(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_23(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_24(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_25(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_26(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_27(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_28(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_29(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_30(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_31(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_32(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_33(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_34(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_35(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_36(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_37(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_38(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_39(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[19] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_40(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_41(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_42(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_43(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_44(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_45(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 2) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_46(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 4) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_47(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 6) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_48(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30]) & 0x03; \
          } while(0)

#define SB_ZF_KAEPSLIMVLANRECORD_GET_PORT_STATE_49(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 2) & 0x03; \
          } while(0)

#endif
#endif
