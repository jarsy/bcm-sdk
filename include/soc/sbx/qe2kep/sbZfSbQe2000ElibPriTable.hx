/*  -*-  Mode:C; c-basic-offset:4 -*- */
/*
 * $Id: sbZfSbQe2000ElibPriTable.hx,v 1.4 Broadcom SDK $
 * $Copyright: (c) 2016 Broadcom.
 * Broadcom Proprietary and Confidential. All rights reserved.$
 */


#ifndef SB_ZF_SB_QE2000_ELIB_PRI_TABLE_H
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_H

#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_SIZE_IN_BYTES 32
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_SIZE 32
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD7_BITS "255:248"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI63_BITS "247:245"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI62_BITS "244:242"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI61_BITS "241:239"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI60_BITS "238:236"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI59_BITS "235:233"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI58_BITS "232:230"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI57_BITS "229:227"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI56_BITS "226:224"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD6_BITS "223:216"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI55_BITS "215:213"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI54_BITS "212:210"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI53_BITS "209:207"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI52_BITS "206:204"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI51_BITS "203:201"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI50_BITS "200:198"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI49_BITS "197:195"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI48_BITS "194:192"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD5_BITS "191:184"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI47_BITS "183:181"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI46_BITS "180:178"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI45_BITS "177:175"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI44_BITS "174:172"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI43_BITS "171:169"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI42_BITS "168:166"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI41_BITS "165:163"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI40_BITS "162:160"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD4_BITS "159:152"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI39_BITS "151:149"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI38_BITS "148:146"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI37_BITS "145:143"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI36_BITS "142:140"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI35_BITS "139:137"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI34_BITS "136:134"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI33_BITS "133:131"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI32_BITS "130:128"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD3_BITS "127:120"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI31_BITS "119:117"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI30_BITS "116:114"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI29_BITS "113:111"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI28_BITS "110:108"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI27_BITS "107:105"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI26_BITS "104:102"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI25_BITS "101:99"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI24_BITS "98:96"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD2_BITS "95:88"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI23_BITS "87:85"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI22_BITS "84:82"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI21_BITS "81:79"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI20_BITS "78:76"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI19_BITS "75:73"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI18_BITS "72:70"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI17_BITS "69:67"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI16_BITS "66:64"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD1_BITS "63:56"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI15_BITS "55:53"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI14_BITS "52:50"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI13_BITS "49:47"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI12_BITS "46:44"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI11_BITS "43:41"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI10_BITS "40:38"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI9_BITS "37:35"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI8_BITS "34:32"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_RSVD0_BITS "31:24"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI7_BITS "23:21"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI6_BITS "20:18"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI5_BITS "17:15"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI4_BITS "14:12"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI3_BITS "11:9"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI2_BITS "8:6"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI1_BITS "5:3"
#define SB_ZF_SB_QE2000_ELIB_PRI_TABLE_PRI0_BITS "2:0"


/**
 * @file sbZfSbQe2000ElibPriTable.h
 *
 * <pre>
 *
 * ==========================================================
 * ==  sbZfSbQe2000ElibPriTable.h - Port Pri Table ==
 * ==========================================================
 *
 * WORKING REVISION: $Id: sbZfSbQe2000ElibPriTable.hx,v 1.4 Broadcom SDK $
 *
 * Copyright (c) Sandburst, Inc. 2005
 * All Rights Reserved.  Unpublished rights reserved under the copyright
 * laws of the United States.
 *
 * The software contained on this media is proprietary to and embodies the
 * confidential technology of Sandburst, Inc. Possession, use, duplication
 * or dissemination of the software and media is authorized only pursuant
 * to a valid written license from Sandburst, Inc.
 *
 * RESTRICTED RIGHTS LEGEND Use, duplication, or disclosure by the U.S.
 * Government is subject to restrictions as set forth in Subparagraph
 * (c) (1) (ii) of DFARS 252.227-7013, or in FAR 52.227-19, as applicable.
 *
 *
 * MODULE NAME:
 *
 *     sbZfSbQe2000ElibPriTable.h
 *
 * ABSTRACT:
 *
 *    ZFrame Definition.  Allow 24 bit RMW of BF Pri Memory
 *
 * LANGUAGE:
 *
 *     ZFrame
 *
 * AUTHORS:
 *
 *     Travis B. Sawyer
 *
 * CREATION DATE:
 *
 *     27-October-2005
 * </pre>
 */
typedef struct _sbZfSbQe2000ElibPriTable {
  uint32 Rsvd7;
  uint32 Pri63;
  uint32 Pri62;
  uint32 Pri61;
  uint32 Pri60;
  uint32 Pri59;
  uint32 Pri58;
  uint32 Pri57;
  uint32 Pri56;
  uint32 Rsvd6;
  uint32 Pri55;
  uint32 Pri54;
  uint32 Pri53;
  uint32 Pri52;
  uint32 Pri51;
  uint32 Pri50;
  uint32 Pri49;
  uint32 Pri48;
  uint32 Rsvd5;
  uint32 Pri47;
  uint32 Pri46;
  uint32 Pri45;
  uint32 Pri44;
  uint32 Pri43;
  uint32 Pri42;
  uint32 Pri41;
  uint32 Pri40;
  uint32 Rsvd4;
  uint32 Pri39;
  uint32 Pri38;
  uint32 Pri37;
  uint32 Pri36;
  uint32 Pri35;
  uint32 Pri34;
  uint32 Pri33;
  uint32 Pri32;
  uint32 Rsvd3;
  uint32 Pri31;
  uint32 Pri30;
  uint32 Pri29;
  uint32 Pri28;
  uint32 Pri27;
  uint32 Pri26;
  uint32 Pri25;
  uint32 Pri24;
  uint32 Rsvd2;
  uint32 Pri23;
  uint32 Pri22;
  uint32 Pri21;
  uint32 Pri20;
  uint32 Pri19;
  uint32 Pri18;
  uint32 Pri17;
  uint32 Pri16;
  uint32 Rsvd1;
  uint32 Pri15;
  uint32 Pri14;
  uint32 Pri13;
  uint32 Pri12;
  uint32 Pri11;
  uint32 Pri10;
  uint32 Pri9;
  uint32 Pri8;
  uint32 Rsvd0;
  uint32 Pri7;
  uint32 Pri6;
  uint32 Pri5;
  uint32 Pri4;
  uint32 Pri3;
  uint32 Pri2;
  uint32 Pri1;
  uint32 Pri0;
} sbZfSbQe2000ElibPriTable_t;

uint32
sbZfSbQe2000ElibPriTable_Pack(sbZfSbQe2000ElibPriTable_t *pFrom,
                              uint8 *pToData,
                              uint32 nMaxToDataIndex);
void
sbZfSbQe2000ElibPriTable_Unpack(sbZfSbQe2000ElibPriTable_t *pToStruct,
                                uint8 *pFromData,
                                uint32 nMaxToDataIndex);
void
sbZfSbQe2000ElibPriTable_InitInstance(sbZfSbQe2000ElibPriTable_t *pFrame);

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[31] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI63(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI62(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[30] = ((pToData)[30] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI61(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[30] = ((pToData)[30] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI60(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI59(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[29] = ((pToData)[29] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI58(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[29] = ((pToData)[29] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI57(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI56(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[28] = ((pToData)[28] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[27] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI55(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI54(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[26] = ((pToData)[26] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI53(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[26] = ((pToData)[26] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI52(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI51(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[25] = ((pToData)[25] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI50(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[25] = ((pToData)[25] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI49(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI48(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[24] = ((pToData)[24] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[23] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI47(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI46(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[22] = ((pToData)[22] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI45(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[22] = ((pToData)[22] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI44(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI43(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[21] = ((pToData)[21] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI42(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[21] = ((pToData)[21] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI41(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI40(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[20] = ((pToData)[20] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[19] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI39(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI38(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[18] = ((pToData)[18] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI37(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[18] = ((pToData)[18] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI36(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI35(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[17] = ((pToData)[17] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI34(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[17] = ((pToData)[17] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI33(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI32(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[16] = ((pToData)[16] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[15] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI31(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[14] = ((pToData)[14] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI30(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[14] = ((pToData)[14] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI29(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[13] = ((pToData)[13] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[14] = ((pToData)[14] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI28(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[13] = ((pToData)[13] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI27(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[13] = ((pToData)[13] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI26(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[12] = ((pToData)[12] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[13] = ((pToData)[13] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI25(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[12] = ((pToData)[12] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI24(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[12] = ((pToData)[12] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[11] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI23(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[10] = ((pToData)[10] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI22(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[10] = ((pToData)[10] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI21(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[9] = ((pToData)[9] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[10] = ((pToData)[10] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI20(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[9] = ((pToData)[9] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI19(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[9] = ((pToData)[9] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI18(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[8] = ((pToData)[8] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[9] = ((pToData)[9] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI17(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[8] = ((pToData)[8] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI16(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[8] = ((pToData)[8] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[7] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI15(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[6] = ((pToData)[6] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI14(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[6] = ((pToData)[6] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI13(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[5] = ((pToData)[5] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[6] = ((pToData)[6] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI12(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[5] = ((pToData)[5] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI11(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[5] = ((pToData)[5] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI10(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[4] = ((pToData)[4] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[5] = ((pToData)[5] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI9(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[4] = ((pToData)[4] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI8(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[4] = ((pToData)[4] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_RSVD0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[3] = ((nFromData)) & 0xFF; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI7(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[2] = ((pToData)[2] & ~(0x07 << 5)) | (((nFromData) & 0x07) << 5); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI6(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[2] = ((pToData)[2] & ~(0x07 << 2)) | (((nFromData) & 0x07) << 2); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI5(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[1] = ((pToData)[1] & ~(0x01 << 7)) | (((nFromData) & 0x01) << 7); \
           (pToData)[2] = ((pToData)[2] & ~ 0x03) | (((nFromData) >> 1) & 0x03); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI4(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[1] = ((pToData)[1] & ~(0x07 << 4)) | (((nFromData) & 0x07) << 4); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI3(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[1] = ((pToData)[1] & ~(0x07 << 1)) | (((nFromData) & 0x07) << 1); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI2(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[0] = ((pToData)[0] & ~(0x03 << 6)) | (((nFromData) & 0x03) << 6); \
           (pToData)[1] = ((pToData)[1] & ~ 0x01) | (((nFromData) >> 2) & 0x01); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI1(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[0] = ((pToData)[0] & ~(0x07 << 3)) | (((nFromData) & 0x07) << 3); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_SET_PRI0(nFromData,pToData,nMaxToDataIndex) \
          do { \
           (pToData)[0] = ((pToData)[0] & ~0x07) | ((nFromData) & 0x07); \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[31] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI63(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI62(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[30] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI61(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[30] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI60(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI59(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[29] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI58(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[29] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI57(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI56(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[28]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[27] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI55(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI54(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[26] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI53(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[26] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI52(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI51(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[25] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI50(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[25] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI49(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI48(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[24]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[23] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI47(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI46(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[22] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI45(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[22] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI44(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI43(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[21] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI42(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[21] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI41(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI40(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[20]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[19] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI39(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI38(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[18] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI37(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[18] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI36(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI35(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[17] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI34(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[17] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI33(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI32(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[16]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[15] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI31(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[14] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI30(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[14] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI29(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[13] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[14] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI28(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[13] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI27(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[13] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI26(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[12] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[13] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI25(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[12] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI24(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[12]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[11] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI23(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[10] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI22(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[10] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI21(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[9] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[10] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI20(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[9] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI19(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[9] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI18(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[8] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[9] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI17(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[8] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI16(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[8]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[7] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI15(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[6] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI14(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[6] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI13(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[5] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[6] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI12(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[5] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI11(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[5] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI10(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[4] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[5] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI9(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[4] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI8(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[4]) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_RSVD0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) (pFromData)[3] ; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI7(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[2] >> 5) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI6(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[2] >> 2) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI5(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[1] >> 7) & 0x01; \
           (nToData) |= (uint32) ((pFromData)[2] & 0x03) << 1; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI4(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[1] >> 4) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI3(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[1] >> 1) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI2(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[0] >> 6) & 0x03; \
           (nToData) |= (uint32) ((pFromData)[1] & 0x01) << 2; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI1(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[0] >> 3) & 0x07; \
          } while(0)

#define SB_ZF_SBQE2000ELIBPRITABLE_GET_PRI0(nToData,pFromData,nMaxFromDataIndex) \
          do { \
           (nToData) = (uint32) ((pFromData)[0]) & 0x07; \
          } while(0)

void sbZfSbQe2000ElibPriTableSet(sbZfSbQe2000ElibPriTable_t *pZf, UINT nIdx, UINT nPri);
UINT sbZfSbQe2000ElibPriTableGet(sbZfSbQe2000ElibPriTable_t *pZf, UINT nIdx);
#endif
